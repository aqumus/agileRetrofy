{
  "name": "retrofy-server",
  "version": "1.0.0",
  "description": "Agile retro board server app",
  "main": "dist/app.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node-dev --respawn --transpile-only src/app.ts",
    "start:prod": "node dist/app.js",
    "build": "tsc",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "fastify": "^3.9.2"
  },
  "devDependencies": {
    "@tsconfig/node12": "^1.0.7",
    "dotenv": "^8.2.0",
    "ts-node-dev": "^1.1.1"
  }
}
